# these are external modules for going with client web application we have to see first api is responding or not by using pip tool request module
import requests

response = requests.get('https://www.python.org/santhu')
print(response)
